# Credentials and API Keys Template
# 
# INSTRUCTIONS:
# 1. Copy this file to 'credentials.txt' in the same directory
# 2. Replace all placeholder values with your actual credentials
# 3. DO NOT commit credentials.txt to version control (it's in .gitignore)
# 4. Keep this example file as a reference for the structure
#
# NOTE: Environment variables take precedence over this file.
#       If you set environment variables, they will be used instead.

# ============================================
# SNOWFLAKE CREDENTIALS (REQUIRED)
# ============================================
# Your Snowflake account identifier (e.g., xy12345.us-east-2.aws or xy12345.east-us-2.azure)
SNOWFLAKE_ACCOUNT=your-account-identifier.region.cloud-provider

# Snowflake username
SNOWFLAKE_USER=your-username

# Snowflake password
SNOWFLAKE_PASSWORD=your-password

# Snowflake warehouse name (default: COMPUTE_WH)
SNOWFLAKE_WAREHOUSE=COMPUTE_WH

# Target database name in Snowflake (default: MIGRATION_DB)
SNOWFLAKE_DATABASE=MIGRATION_DB

# Target schema name in Snowflake (default: PUBLIC)
# Note: Schema can also be specified in Phase 3 instructions
SNOWFLAKE_SCHEMA=PUBLIC

# ============================================
# AZURE OPENAI CREDENTIALS (REQUIRED)
# ============================================
# Your Azure OpenAI endpoint URL
AZURE_OPENAI_ENDPOINT=https://your-resource-name.openai.azure.com/

# Azure OpenAI API key
AZURE_OPENAI_API_KEY=your-azure-openai-api-key

# Azure OpenAI API version (default: 2024-12-01-preview)
AZURE_OPENAI_API_VERSION=2024-12-01-preview

# Azure subscription ID (optional, for some operations)
AZURE_SUBSCRIPTION_ID=your-subscription-id

# ============================================
# AZURE AI SERVICES PROJECT CREDENTIALS (OPTIONAL)
# ============================================
# Azure AI Services project endpoint (if using Azure AI Services)
AZURE_PROJECT_ENDPOINT=https://your-project.services.ai.azure.com/api/projects/your-project-name

# Azure AI Services project key
AZURE_PROJECT_KEY=your-project-key

# ============================================
# POSTGRESQL CREDENTIALS (OPTIONAL - for source database)
# ============================================
# These are typically provided in Phase 1 instructions, but can be set here as defaults
POSTGRES_HOST=your-postgres-host.database.azure.com
POSTGRES_USER=your-postgres-username
POSTGRES_PASSWORD=your-postgres-password
POSTGRES_DB=your-database-name
POSTGRES_PORT=5432

# ============================================
# TERADATA CREDENTIALS (OPTIONAL - for source database)
# ============================================
# Teradata host (format: hostname.env.clearscape.teradata.com)
TERADATA_HOST=your-host.env.clearscape.teradata.com
TERADATA_USER=your-teradata-username
TERADATA_PASSWORD=your-teradata-password

# ============================================
# AWS CREDENTIALS (OPTIONAL)
# ============================================
# AWS access key ID (if using AWS services)
AWS_ACCESS_KEY_ID=your-aws-access-key-id
AWS_SECRET_ACCESS_KEY=your-aws-secret-access-key
AWS_DEFAULT_REGION=us-east-1

# ============================================
# AZURE KEY VAULT (OPTIONAL)
# ============================================
# Azure Key Vault URL (if using Key Vault for credential management)
KEY_VAULT_URL=https://your-keyvault-name.vault.azure.net/

